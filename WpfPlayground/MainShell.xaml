<Window x:Class="WpfPlayground.MainShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Title="MainShell" Height="300" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Margin="0 10 0 0" Content="ChangeTheme" Click="Button_Click" Grid.ColumnSpan="2" />
        <ContentControl Name="LeftMenu" 
                        prism:RegionManager.RegionName="LeftMenu"
                        VerticalAlignment="Center"
                        Grid.Row="1"/>
        <ContentControl Name="MainContent"
                        prism:RegionManager.RegionName="MainContent"
                        Grid.Column="1"
                        Grid.Row="1"/>
    </Grid>
</Window>
